//runs before server start, will finish before any other setup happens, use this to configure any config that needs to be set before the server starts
const mongoose = require("mongoose");
const config = require("./config/config");
module.exports = async () => {
    
}
